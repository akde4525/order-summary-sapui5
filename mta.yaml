_schema-version: "3.2"
ID: ordersummary
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: ordersummary-destination-content
  type: com.sap.application.content
  requires:
  - name: ordersummary-destination-service
    parameters:
      content-target: true
  - name: ordersummary-repo-host
    parameters:
      service-key:
        name: ordersummary-repo-host-key
  - name: ordersummary-uaa
    parameters:
      service-key:
        name: ordersummary-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: ordersummary_html_repo_host
          ServiceInstanceName: ordersummary-html5-service
          ServiceKeyName: ordersummary-repo-host-key
          sap.cloud.service: ordersummary
        - Authentication: OAuth2UserTokenExchange
          Name: ordersummary_uaa
          ServiceInstanceName: ordersummary-xsuaa-service
          ServiceKeyName: ordersummary-uaa-key
          sap.cloud.service: ordersummary
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: ordersummary-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: ordersummary-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - ordersummary.zip
      name: ordersummary
      target-path: resources/
- name: ordersummary
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: ordersummary-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: ordersummary-destination-service
    service-plan: lite
- name: ordersummary-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: ordersummary-xsuaa-service
    service-plan: application
- name: ordersummary-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: ordersummary-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
